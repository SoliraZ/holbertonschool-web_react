# React Hooks — Task 3

## Objective

Convert `Notifications` from a class component (`PureComponent`) to a **functional component** wrapped with **`memo`** and a custom comparison function.

## Requirements covered

- Function syntax instead of class syntax
- `memo` with custom comparator: re-render only when `notifications.length` or `displayDrawer` changes
- All previous notification behaviors preserved

## Files

- `dashboard/src/Notifications/Notifications.jsx`
- `dashboard/src/Notifications/Notifications.spec.js`
