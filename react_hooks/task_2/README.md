# React Hooks — Task 2

## Objective

Convert `NotificationItem` from a class component (`PureComponent`) to a **functional component** wrapped with **`memo`** for performance optimization.

## Requirements covered

- Function syntax instead of class syntax
- `memo` replaces `PureComponent` behavior
- Same prop structure and rendering behavior
- `markAsRead` click handler preserved

## Files

- `dashboard/src/Notifications/NotificationItem.jsx`
- `dashboard/src/Notifications/NotificationItem.spec.js`
