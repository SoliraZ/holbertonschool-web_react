# React state — Holberton School

This folder contains the **React state** project (local state, event handlers, and component interactions).

Each task lives in its own directory (`task_0`, `task_1`, …) with a Vite dashboard app under `dashboard/`.
