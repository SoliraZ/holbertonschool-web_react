# Task 0 — Set TailwindCSS

Set up TailwindCSS v4 using `@tailwindcss/vite` without creating a Tailwind config file.

Configure Roboto as the default font globally in `src/main.css` and import Roboto weights in `src/main.jsx`.
